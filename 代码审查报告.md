# 绘语 ArtTalk / huiyu 项目代码审查报告

- 审查日期：2026-08-05
- 审查范围：im-backend（124 个 Java 文件）、im-web（84 个源文件）、im-admin（21 个源文件）、app/docker（Compose/Nginx/MinIO/脚本）、.github/workflows、SQL 迁移脚本、项目开发架构文档.md
- 审查方式：基于实际代码逐文件阅读（9 个领域并行审查），关键结论经二次抽查核实；本次审查未修改任何文件
- 严重等级定义：P0=系统不可用/严重数据泄漏/大规模数据损坏；P1=核心功能错误/严重越权/消息或文件数据丢失；P2=局部功能异常/稳定性问题/明显维护风险；P3=一般代码质量/规范/低风险优化

---

## 1. 项目总体评价

整体质量明显高于一般内网项目，安全基本功扎实：

- 认证吊销链完整（登出黑名单 + tokenVersion 递增 + 每请求 DB 比对），JWT 密钥启动 fail-fast 校验，无硬编码密钥；
- `/api/admin/**` 由 SecurityConfig 统一强制 ROLE_ADMIN 且角色取自 DB，管理端越权防护链完整；
- 文件下载鉴权（canAccessFile）、魔数嗅探、nosniff/CSP sandbox 响应头、路径穿越防护均闭合；
- 消息幂等（uk_client_msg 三重防护）、群管理权限矩阵、分片上传行锁、配额 FOR UPDATE 防超卖均已核实无恙；
- Electron 主进程加固到位（contextIsolation/sandbox/IPC sender 校验/导航白名单），MinIO bucket 非 public、不使用预签名 URL。

但存在 2 个 P1 级缺陷：全新部署必然失败（Flyway 迁移链断裂）、登出不清理聊天状态（跨账号消息可见）。另有约 17 个 P2 集中在：单聊并发/退出语义、消息分页与多端同步、部门管理、Admin 前端字段错位、部署与 CI 安全。文档（协议、技术栈、审计能力描述）与代码漂移明显。

## 2. 项目架构与核心调用链

```
im-web (Electron+Vue3)                im-admin (Vue3+Element Plus)
   │ HTTPS/WSS                              │ HTTPS
   ▼                                        ▼
nginx :443 (TLS, /api /ws /downloads, limit_req 仅限更新下载)
   │
   ▼
im-backend (Spring Boot 3.4, Java 17)
  AuthController → AuthServiceImpl(BCrypt) → JwtUtil(HS256,7天,tokenVersion)
  JwtAuthenticationFilter → TokenAuthenticationService(验签→Redis黑名单→DB用户/版本)
  WS: /api/auth/ws-ticket(一次性ticket,Redis,30s) → WebSocketHandshakeInterceptor
      → ImWebSocketHandler → MessageServiceImpl(成员校验→幂等→im_message+delivery+lastMessage @Transactional)
  HTTP: ConversationController/FileController → Service → MyBatis-Plus Mapper
   │              │                │
 MySQL 13表     Redis(黑名单/ticket/在线)   MinIO或本地存储(FileStorageRouter)
```

- 消息投递：WS 实时推在线成员；离线成员靠 `GET /messages/pending`（delivery.delivered=0）+ ACK 补齐；已读用 delivery.read_status + member.last_read_time 双轨。
- 文件：小文件直传（≤100MB）/ 分片（64MB 块，最大 2GB，FOR UPDATE 串行，SHA-256 校验，同会话秒传）；下载由后端代理并逐文件鉴权。
- 更新链：GitHub Actions（self-hosted runner，Authenticode 签名）→ publish-update.ps1（校验 SHA512，原子切换）→ nginx 静态分发 → electron-updater（强制 HTTPS）。
- DB 迁移：docker-entrypoint-initdb.d 执行 `sql/schema.sql` 建基线 + Flyway `db/migration`（baseline 20260726，V20260727~V20260804 共 6 个）。

---

## 3. 已确认问题清单

> 无已确认 P0。编号按严重等级排序。

### 问题编号：REV-001
- 严重等级：P1
- 审查状态：已确认
- 所属模块：数据库/部署
- 文件路径：`app/im-backend/sql/schema.sql`、`app/im-backend/im-server/src/main/resources/db/migration/V20260730__add_group_avatar_state.sql`、`V20260731__add_group_create_request_id.sql`
- 类、方法或组件：Flyway 迁移链 vs docker-entrypoint 基线
- 问题说明：schema.sql 已包含 V20260730（avatar_type 等 3 列，schema.sql:44-46）与 V20260731（create_request_id + uk_owner_create_request，:48,58）的全部内容，但两个迁移无幂等守卫（V20260727 有守卫，证明作者知情）。全新环境：MySQL 初始化执行最新 schema.sql → Flyway baseline 20260726 → 执行 V20260730 时 ALTER ADD 重复列报 1060 → Flyway 失败 → Spring 启动失败 → `restart: always` crash-loop。
- 触发条件：任何全新部署（新环境/删数据卷重建/灾备恢复）。
- 实际影响：整套系统在新环境永远起不来。存量环境不受影响（解释了现网为何正常）。
- 证据：schema.sql:44-48,58 与 V20260730:1-4/V20260731:1-4 内容重复；application.yml:25-26 `baseline-version: 20260726`；docker-compose.yml:17 挂载 schema.sql 到 initdb.d。
- 建议修复方案：(a) schema.sql 回滚为 20260726 基线结构，增量全交 Flyway；(b) 同时给 V20260730/V20260731 补 information_schema 幂等守卫；CI 加"空库起容器跑通 Flyway"冒烟。
- 建议验证方法：删除 MySQL 数据卷后 `docker compose up`，确认 Flyway 全部成功无 failed 记录。
- 是否建议立即修复：是

### 问题编号：REV-002
- 严重等级：P1
- 审查状态：已确认
- 所属模块：im-web 前端
- 文件路径：`app/im-web/src/stores/auth.ts`、`app/im-web/src/views/Chat.vue`、`app/im-web/src/stores/chat.ts`
- 类、方法或组件：`authStore.logout()` / `handleLogout()` / useChatStore
- 问题说明：登出只清 token/user/userProfiles 和两个 localStorage 键，完全不清 chatStore（conversations、messages、unreadCounts、currentConversation）。chat.ts 无任何 reset 方法，handleLogout 也不调用。
- 触发条件：同设备账号 A 退出 → 账号 B 登录。
- 实际影响：B 登录后 `currentMessages` 直接渲染 A 最后打开会话的聊天记录；B 非该会话成员时 REST 403 后数据不被覆盖，A 的消息持续可见直到手动切换。跨账号信息泄露 + 状态污染。
- 证据：auth.ts:87-98（logout 无 chatStore 调用）；Chat.vue:3550-3558（handleLogout 全文）；chat.ts:546-575（无 reset 导出）。
- 建议修复方案：chatStore 增加 `reset()`，在 handleLogout 和 `watch(isLoggedIn, false)` 中调用。
- 建议验证方法：A 登录打开群聊 → 退出 → B 登录，观察挂载瞬间是否闪现 A 的消息。
- 是否建议立即修复：是

### 问题编号：REV-003
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-会话
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/service/impl/ConversationServiceImpl.java`
- 类、方法或组件：`createConversation`（单聊分支）/ `findExistingSingleChat` :151-193, 627-652
- 问题说明：单聊去重为"先 SELECT 再 INSERT"的 TOCTOU，无锁、无 DB 唯一约束（uk_conv_user 防不了平行会话），也无群聊的 requestId 幂等。
- 触发条件：双方同时发起单聊；客户端双击/超时重试。
- 实际影响：同一对用户产生两个平行单聊，消息历史分裂，会话列表出现重复条目。
- 证据：ConversationServiceImpl.java:163-172（先查后插）；:627-652（普通 selectList 无锁）；schema.sql:61-73 无单聊成员对唯一键。
- 建议修复方案：以 `min(u1,u2)-max(u1,u2)` 建唯一键表/唯一索引，撞键回查复用；或客户端传 requestId 复用群聊幂等机制。
- 建议验证方法：两线程并发创建同一对用户单聊各 10 次，断言只有一个 type=1 会话。
- 是否建议立即修复：是

### 问题编号：REV-004
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-会话
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/service/impl/ConversationServiceImpl.java`
- 类、方法或组件：`removeMember`（单聊自移除分支）:362-402
- 问题说明：单聊成员可移除自己（isSelf 放行），member 行被物理删除；此后 `findExistingSingleChat` 因要求双方 member 行都在而查不到旧会话 → 新建平行会话，旧历史对退出方永久不可达（数据仍在库中）。
- 触发条件：用户在单聊上"删除会话/退出"后再次发起聊天。
- 实际影响：单聊历史分裂、旧记录不可见，正是 findExistingSingleChat 注释声称要避免的问题。
- 证据：:386 放行 isSelf；:390 `deleteById`；:640-643 去重查询必查空。
- 建议修复方案：单聊不允许删 member 行，改为每用户"隐藏"标志。
- 建议验证方法：A 退出单聊后再发起 A→B 会话，断言复用原会话且历史可查。
- 是否建议立即修复：是

### 问题编号：REV-005
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-消息
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/service/impl/MessageServiceImpl.java`、`app/im-backend/sql/schema.sql`
- 类、方法或组件：`getMessages` :97-105
- 问题说明：排序仅 `ORDER BY create_time DESC`（DATETIME 秒级，无 id 兜底），游标却是 `id < beforeMessageId`，排序键与游标键不一致。同秒多条消息或 id 序与时间序错位（并发发送 :177/:181）时，分页重复/遗漏，游标永久跳过错位消息。
- 触发条件：群聊同秒 ≥2 条消息（高频场景常态）。
- 实际影响：历史消息跨页重复或永久缺页。
- 证据：:100 `wrapper.lt(Id, beforeMessageId)`；:102 `orderByDesc(CreateTime)` 无二次排序；schema.sql:83 `DATETIME` 无小数秒。
- 建议修复方案：`orderByDesc(create_time).orderByDesc(id)` + `(create_time,id)` 复合游标；或 create_time 升 DATETIME(3)。
- 建议验证方法：构造同秒且 id 乱序的数据，断言两页并集=全集无重复。
- 是否建议立即修复：是

### 问题编号：REV-006
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-消息/WS
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/websocket/ImWebSocketHandler.java`、`MessageServiceImpl.java`
- 类、方法或组件：`pushMessageToConversationMembers` :350-354 / `createDeliveryRows` :353
- 问题说明：发送者 delivery 行直接置 delivered=1，WS 推送又显式排除发送者，pending 只拉 delivered=0 → 同账号其它在线端永远收不到自己发出的消息。
- 触发条件：系统明确支持多端（每用户多 session），从 A 端发消息。
- 实际影响：多端已发消息同步断裂，B 端只能手动刷新才看到自己发的消息。
- 证据：MessageServiceImpl.java:353（sender delivered=1）；ImWebSocketHandler.java:351（`!memberId.equals(senderId)`）；:240（pending 过滤 delivered=0）。
- 建议修复方案：推送改为 sendToUser(senderId,...) 覆盖全部端，客户端按 clientMsgId 去重。
- 建议验证方法：同账号两个 WS 连接，一端发消息，断言另一端收到。
- 是否建议立即修复：是

### 问题编号：REV-007
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-WS
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/websocket/WebSocketSessionManager.java`
- 类、方法或组件：`removeSession` :47-57 / `addSession` :38
- 问题说明：removeSession 是 check-then-act：`isEmpty()` 判断与 `sessions.remove(userId, set)` 之间，并发 addSession（computeIfAbsent 命中同一 Set 对象）后 remove(key,value) 仍成功 → 新 session 的 Set 被整体孤立，在线用户被判离线。
- 触发条件：断连回调与重连并发（网络抖动常态）。
- 实际影响：消息推送静默丢失、Redis 在线标记被清、广播错误"离线"，需再次重连恢复。
- 证据：:52-55 非原子组合 vs :38 computeIfAbsent。
- 建议修复方案：改用 `sessions.compute(userId, (k,set)->...)` 原子化删除+空判。
- 建议验证方法：并发单测 A remove 最后 session / B add，断言 isOnline=true。
- 是否建议立即修复：是

### 问题编号：REV-008
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-文件
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/service/FileRetentionService.java`、`FileUploadTaskService.java`
- 类、方法或组件：`cleanupExpiredUploadTasks` :66-86 vs `completeTask` :211-284
- 问题说明：清理任务无行锁读 UPLOADING 过期任务，先删存储侧分片与最终合并对象再置 ABORTED；completeTask 持行锁做 compose+SHA-256（大文件数十秒）。任务在执行中跨过过期点恰逢每日清理时，刚合并好的对象被删 → im_file 元数据指向不存在对象。
- 触发条件：任务过期点落在 cleanup cron（默认 3:15）窗口且 completeTask 跨过期点执行。
- 实际影响：用户文件静默丢失（下载 500）；或产生永不清理的孤儿分片。
- 证据：FileRetentionService.java:67-84（无锁 selectList→deleteQuietly(objectKey)→updateById）；FileUploadTaskService.java:212,236（锁只护 DB 行）。
- 建议修复方案：cleanup 逐任务 FOR UPDATE；或先 CAS 状态置 ABORTED 成功后再删存储，跳过 file_id 非空任务。
- 建议验证方法：mock 阻塞中的 completeTask 并发跑 cleanup，断言最终对象未被误删。
- 是否建议立即修复：是

### 问题编号：REV-009
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端+Admin-部门
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/service/impl/DeptServiceImpl.java`、`app/im-admin/src/views/DeptManage.vue`
- 类、方法或组件：`DeptServiceImpl.delete` :67-70 / `handleDelete` :215-233
- 问题说明：删除部门是无条件物理 deleteById，不查子部门也不查在职用户；parent_id 无外键。删后子部门悬空、buildTree 从 parentId=0 递归 → 整棵子树从组织树消失（数据残留），用户 dept_id 被 FK SET NULL 静默清空。前端确认文案还暗示操作安全。
- 触发条件：管理员删除有子部门/有用户的部门。
- 实际影响：组织数据幽灵化，需手工 SQL 修复。
- 证据：DeptServiceImpl.java:68-70 无前置校验；schema.sql:36 parent_id 仅普通索引。
- 建议修复方案：删除前有子部门或有用户则 400 拒绝；前端文案说明后果。
- 建议验证方法：建父-子部门删除父部门，观察子部门消失。
- 是否建议立即修复：是

### 问题编号：REV-010
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Admin-部门
- 文件路径：`app/im-admin/src/views/DeptManage.vue`、`DeptServiceImpl.java`
- 类、方法或组件：编辑弹窗上级部门选择器 :65-75 / `update` :61-65
- 问题说明：编辑部门时上级部门选择器不排除自身及子孙，后端也不校验 parentId → 可形成环，该分支从树查询不可达，整支"消失"。
- 触发条件：把部门 A 的上级设为 A 或 A 的后代并保存。
- 实际影响：子树孤儿化，只能改库恢复。
- 证据：DeptManage.vue:66-74 `:data="treeData"` 未过滤；后端 update/create 仅 updateById。
- 建议修复方案：前端剔除自身及子孙节点；后端校验 parentId 不构成环。
- 建议验证方法：将部门父级设为自身保存，刷新树观察分支消失。
- 是否建议立即修复：是

### 问题编号：REV-011
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Admin-用户管理
- 文件路径：`app/im-admin/src/views/UserManage.vue`、`AdminUserController.java`、`SysUser.java`
- 类、方法或组件：表格列 :40,58 / `pageUsers`
- 问题说明："部门"列读 `row.deptName`、"创建时间"列绑 `createdAt`，后端返回的 SysUser 中两者均不存在（只有 createTime，无 deptName）。
- 触发条件：打开用户管理页，100% 复现。
- 实际影响：部门列恒 `-`、创建时间恒空白。
- 证据：UserManage.vue:40,58；SysUser.java:18-32；AdminUserController.java:42-47 返回原始实体。
- 建议修复方案：后端返回带 deptName 的 VO；前端列改绑 createTime。
- 建议验证方法：`GET /api/admin/users/page` 检查返回 JSON 键名。
- 是否建议立即修复：是

### 问题编号：REV-012
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Admin-部门
- 文件路径：`app/im-admin/src/views/DeptManage.vue`、`DeptVO.java`
- 类、方法或组件：状态标签 :38-44 / `handleEdit` :172
- 问题说明：部门树读 `data.status`，但 DeptVO 只有 id/name/parentId/sortOrder/children 五字段 → 所有部门恒显示"禁用"，编辑弹窗状态无选中项（因 updateById 忽略 null，不会误清数据）。
- 触发条件：打开部门管理页即现。
- 实际影响：状态显示与编辑交互失效。
- 证据：DeptManage.vue:38-44；DeptServiceImpl.convertToVO :85-92 未填 status。
- 建议修复方案：DeptVO 增加 status 并填充。
- 建议验证方法：`GET /api/admin/depts/tree` 检查 JSON 键。
- 是否建议立即修复：是

### 问题编号：REV-013
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Admin-认证
- 文件路径：`app/im-admin/src/api/index.ts`
- 类、方法或组件：axios 响应拦截器 :50-56
- 问题说明：所有 401 一律提示"登录已过期"并清登录态，吞掉后端真实消息；而后端密码错误正是抛 401 → 输错密码提示"登录已过期"，严重误导。
- 触发条件：登录页输错密码。
- 实际影响：错误提示完全错误，排障误导。
- 证据：api/index.ts:51-56 switch 覆盖已取出的 msg；AuthServiceImpl.java:56,60 `BusinessException(401,"用户名或密码错误")`。
- 建议修复方案：优先展示 `data.message`；仅非 /api/auth/login 路径按过期处理。
- 建议验证方法：登录页输错密码观察提示。
- 是否建议立即修复：是

### 问题编号：REV-014
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-认证
- 文件路径：`app/im-backend/im-server/src/main/java/com/im/server/service/impl/AuthServiceImpl.java`、`app/docker/nginx/nginx.conf`
- 类、方法或组件：`login` :50-61
- 问题说明：登录无失败计数/锁定/限流/验证码；nginx 的 limit_req 仅覆盖更新下载路径，不含 /api/auth/login。密码策略仅长度 12-128 无复杂度。
- 触发条件：内网攻击者在线爆破/撞库。
- 实际影响：弱口令可被枚举，成功即得 7 天全功能 JWT。
- 证据：login() 仅查用户+matches，无任何计数；nginx.conf:13,64 limit_req 位置。
- 建议修复方案：Redis 实现"用户名+IP"失败计数与退避锁定；或 nginx 加 limit_req；记审计日志。
- 建议验证方法：脚本连发 20 次错误密码，观察 429/锁定。
- 是否建议立即修复：是

### 问题编号：REV-015
- 严重等级：P2
- 审查状态：已确认
- 所属模块：后端-安全
- 文件路径：`app/im-backend/im-common/src/main/java/com/im/common/config/WebConfig.java`
- 类、方法或组件：`addCorsMappings` :19-26
- 问题说明：`allowedOriginPatterns("*")` + `allowCredentials(true)`：反射任意 Origin 且允许凭证。当前认证靠 Authorization 头直接可利用性低，但 token 一旦泄露（XSS/日志）任意源即可跨域读全部 API；未来引入 Cookie 即变 P0。
- 触发条件：恶意站点持有/窃取到任一有效 token。
- 实际影响：跨域读取全部 IM 数据；防御纵深削弱。
- 证据：WebConfig.java:20-25。
- 建议修复方案：改为内网域名白名单，或去掉 allowCredentials；WS 同理收窄。
- 建议验证方法：带恶意 Origin 发 OPTIONS 预检，确认不再反射。
- 是否建议立即修复：是

### 问题编号：REV-016
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Docker/部署
- 文件路径：`app/docker/docker-compose.intranet.yml`
- 类、方法或组件：minio 服务 ports :56-57
- 问题说明：MinIO 控制台以明文 HTTP 绑定内网 LAN 地址（默认 172.16.59.253:9001），登录用的是 root 凭据，明文过网。
- 触发条件：内网嗅探/ARP 欺骗 + 管理员登录控制台。
- 实际影响：MinIO root 泄露 → 全部聊天文件可读改删。
- 证据：:57 `${MINIO_CONSOLE_BIND_IP:-172.16.59.253}:9001:9001`；:55 未启用 MinIO TLS。
- 建议修复方案：console 改绑 127.0.0.1 + SSH 转发；或置于 nginx 后加 TLS+IP 白名单。
- 建议验证方法：内网其它主机访问 9001 应不可达。
- 是否建议立即修复：是

### 问题编号：REV-017
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Docker/部署
- 文件路径：`app/docker/Dockerfile.backend`、`docker-compose.intranet.yml`
- 类、方法或组件：运行阶段镜像 :11-17
- 问题说明：后端容器以 root 运行（无 USER 指令，compose 未设 user:），且宿主机数据目录 bind mount 到 /app/upload → 容器被攻破即对宿主数据目录有 root 等效写权限。MinIO 服务已设 `user: "1000:1000"`，标准不一。
- 触发条件：后端进程被 RCE 攻破。
- 实际影响：逃逸与篡改面扩大。
- 证据：Dockerfile.backend:11-17 无 USER；intranet compose:92-95 bind mount。
- 建议修复方案：Dockerfile 建非 root 用户并 USER 切换。
- 建议验证方法：`docker exec im-backend id` 非 uid 0。
- 是否建议立即修复：是

### 问题编号：REV-018
- 严重等级：P2
- 审查状态：已确认
- 所属模块：CI/CD
- 文件路径：`.github/workflows/desktop-release.yml`
- 类、方法或组件：build-sign-publish run 脚本 :43-45,49,54,71
- 问题说明：`inputs.version` 自由文本以 `${{ }}` 直接插值进 PowerShell 脚本，格式校验本身也在插值后的脚本内 → 注入代码在校验前即被解释。在 self-hosted runner 上以可访问代码签名证书（CSC_LINK/CSC_KEY_PASSWORD）的上下文执行。
- 触发条件：有 write 权限的账号（或被盗 token）触发 workflow 传恶意 version。
- 实际影响：签名密钥失窃、runner 被持久化。
- 证据：:43 `if ('${{ inputs.version }}' -notmatch ...)` 等多处。
- 建议修复方案：输入经 `env:` 传递，脚本内只用 `$env:VERSION_INPUT`，先校验后使用。
- 建议验证方法：以注入型 version 在测试分支触发，确认被拒绝。
- 是否建议立即修复：是

### 问题编号：REV-019
- 严重等级：P2
- 审查状态：已确认
- 所属模块：Electron 主进程
- 文件路径：`app/im-web/electron/localMessages.ts`
- 类、方法或组件：`mutateStore`/`writeStoreUnlocked` :80-91,103-113
- 问题说明：本地消息缓存为"单一大 JSON + 全量读解密-改-加密-全量重写"，无容量上限、无分片、无自动清理。每条新消息（含每条 WS 推送）触发整文件重写，写入放大 O(总消息量)。
- 触发条件：高频群聊正常使用，消息量线性增长。
- 实际影响：缓存数十 MB 后每条消息的 I/O 与 CPU 持续劣化，最终可撑满用户磁盘。
- 证据：localMessages.ts:104-107（每次变异全量重写）；全文无条数/大小上限。
- 建议修复方案：按会话分桶分文件；或每会话截断最近 N 条；append-only + compaction。
- 建议验证方法：写入 5 万条消息后测单次 upsert 耗时与文件大小曲线。
- 是否建议立即修复：是

### P3 已确认 / 改进建议（REV-020 ~ REV-056，简列）

**REV-020（P3 已确认｜后端-消息）** `MessageServiceImpl.getPendingMessages` :235-258 + `ConversationServiceImpl.removeMember` :390：被踢用户 delivery 行不清理，/pending 不复核成员身份 → 被踢后仍可拉到被踢前未投递消息；重新入群会把旧消息批量当新消息弹出。证据：pending 查询仅 userId+delivered=0。修复：removeMember 清理 delivered=0 的 delivery 或 pending 联查 member。验证：A 离线→B 发消息→踢 A→A 拉 pending 应为空。是否立即修复：否。

**REV-021（P3 已确认｜后端-消息）** `markConversationRead` :295-328：无论客户端报的 lastReadMessageId 边界是哪条，member.lastReadTime 都刷成 now()，与 delivery 按边界更新两套口径矛盾 → 传旧边界时未读角标清零但消息级仍显示未读。修复：lastReadTime 取 max(原值, 边界消息 createTime)。验证：m1..m5 只报 m2，断言未读=3。否。

**REV-022（P3 已确认｜后端-会话）** `ConversationServiceImpl.addMembers/removeMember/updateSettings/updateMemberRole` :340,399,491,517：事务提交前同步推送 CONVERSATION_UPDATED，新成员收到推送立即拉详情会因事务未提交 403；回滚则推送了不存在状态。同类方法已用 afterCommit，此处不一致。修复：统一 runAfterCommit。否。

**REV-023（P3 已确认｜后端-消息+DB）** `MessageServiceImpl.validateSendRequest` :641 + schema.sql:80：content 按字符数限 60000，但 TEXT 列上限 65535 字节，utf8mb4 下约 1.6 万 emoji 或 2.2 万汉字即超限 → Data truncation 500 全事务回滚，客户端"发出无响应"。群名/公告也无长度校验。修复：列改 MEDIUMTEXT 或按 UTF-8 字节数校验。验证：发 3 万汉字消息。否。

**REV-024（P3 已确认｜后端-异常）** `GlobalExceptionHandler` :62：DuplicateKeyException/DataIntegrityViolationException 无映射一律 500。触发点已核实：Admin 建重名用户（UserServiceImpl.create 无查重）、群创建幂等键竞态、用户设置并发首存、空串 clientMsgId 撞 uk_client_msg。修复：加 409 映射 + 用户名查重。验证：重名建用户应 409。否。

**REV-025（P3 已确认｜后端-并发）** `MessageServiceImpl.sendMessage/markConversationRead` :151,204-205,325-328：用方法开头读出的完整 member 实体 updateById 整行覆写，与置顶/免打扰/角色调整/群主转让并发时 lost update，用户设置被静默回滚。修复：改 UpdateWrapper 只 set last_read_time。否。

**REV-026（P3 已确认｜后端-文件）** `FileMetadataService.incrementDownloadCount` :112-119：读-改-写无原子性，并发下载丢失更新（仅统计不准）。修复：一条原子 UPDATE ...+1。否。

**REV-027（P3 已确认｜后端-认证）** `AuthServiceImpl.login` :55-61：用户不存在时提前返回，存在时多一次 BCrypt → 响应时间差可枚举用户名（消息文案已统一）。修复：不存在时对 dummy 哈希执行 matches。否。

**REV-028（P3 已确认｜后端-用户）** `UserController.search/getProfile` :75-81,51-54：搜索 status 传 null 不过滤，禁用账号仍可被搜到；任意登录用户可读他人邮箱/手机号（可能是设计意图，建议产品确认）。修复：搜索默认 status=1。否。

**REV-029（P3 已确认｜后端-文件）** `FileController.uploadAvatar` :99-107：换头像旧文件不退役（群头像流程有 retireFile，用户头像没有），永久残留且计入配额，无任何回收入口。修复：提交后 retireFile 旧 fileId。否。

**REV-030（P3 已确认｜后端-文件）** `FileQuotaService` + `FileUploadMapper.sumActiveBytesByUploader` :33-35：过期上传任务立即释放配额，但分片对象每日才删 → 恶意循环"建任务→传满→放弃"可使实际存储数倍于配额（有界、每日自愈）。修复：cleanup 改短间隔或 UPLOADING 即计配额。否。

**REV-031（P3 已确认｜后端-文件）** `FileUploadTaskService.completeTask` :283-284：成功后只删存储分片对象不删 im_file_upload_part 行（cancelTask 有删行），表单调膨胀；deleteQuietly 吞异常无告警。修复：对齐 cancelTask 删行 + warn 日志。否。

**REV-032（P3 已确认｜im-web）** `Chat.vue onMounted` :3584-3598 + `auth.ts loadFromStorage` :125-131：冷启动 token 失效时静默清状态但不跳 /login，用户停在无数据骨架页无提示。修复：init 后未登录则 router.push('/login')。否。

**REV-033（P3 已确认｜im-web）** `stickers.ts parseStickerContent` :44-75 + `Chat.vue` :2672：内置贴纸/IMAGE 回退直接使用消息中发送方可控的 url 作为 `<img src>` 自动加载 → 内部恶意用户可让全员客户端对任意内网地址发起 GET（探测/日志污染/简单 CSRF）。无脚本执行。修复：贴纸仅按 id 匹配本地表，忽略 url。否。

**REV-034（P3 已确认｜im-web/Electron）** `UpdateCenter.vue` :52-53 + `updater.ts` :483：http 服务器地址下 `updater:configure` throw，渲染端 `void update.initialize()` 未 catch → 更新功能静默失效无任何提示。修复：try/catch 并展示错误。否。

**REV-035（P3 已确认｜后端-WS）** `WebSocketConfig` :42：`setAllowedOrigins("*")` 放弃 Origin 校验；当前靠一次性 ticket 兜底无直接危害，属纵深缺口。修复：配内网来源白名单。否。

**REV-036（P3 改进建议｜后端-WS）** `WebSocketHandshakeInterceptor` :38：ticket 走 URL query 会进 nginx access log（一次性+30s TTL 已大幅缓解）。修复：改 Sec-WebSocket-Protocol 传递或日志脱敏。否。

**REV-037（P3 改进建议｜后端-认证）** `JwtUtil.EXPIRATION` :30：access token 7 天偏长（吊销链完整，但静默泄露窗口长；已有 /refresh 基础设施）。修复：缩短至 2-12 小时。否。

**REV-038（P3 改进建议｜后端-配置）** `application.yml` :6：`useSSL=false&allowPublicKeyRetrieval=true`，DB 流量明文。内网可接受，建议记录为已知风险或启用 SSL。否。

**REV-039（P3 已确认｜Docker/nginx）** `nginx.conf` :7-28：未设 `server_tokens off`，泄露 nginx 版本（含 dev 配置）。修复：http 块加一行。否。

**REV-040（P3 已确认｜CI）** `verify.yml` build job 无 `permissions:` 声明，GITHUB_TOKEN 落回默认权限（同文件 dependency-review 已显式 contents: read，不一致）。修复：顶层加 `permissions: contents: read`。否。

**REV-041（P3 已确认｜CI）** desktop-release.yml/verify.yml 第三方 action 均按 @v4 tag 引用未 pin commit SHA，release 流程处理签名密钥敏感度高。修复：至少 release workflow pin SHA。否。

**REV-042（P3 改进建议｜Docker）** `mysql-init/00-users.sh` :17：密码转义仅处理单引号，含反斜杠序列时 SQL 语义被改（授权本身已最小化，无 GRANT ALL）。修复：口令字符集校验。否。

**REV-043（P3 改进建议｜后端-性能）** `ConversationServiceImpl.listConversations` :103-138 等：会话列表 N+1（每会话未读 count + 每成员 user 查询）+ @未读全量捞 TEXT 消息逐条解析 JSON + delivery 逐条 insert。200 人规模当前可接受，高频路径性能债。修复：批量 IN 查询、mention 落列、batch insert。否。

**REV-044（P3 改进建议｜后端-文件）** 事务内存储 I/O：配额行锁持有整个 128MB 上传 I/O；completeTask 行锁内对 2GB 文件算 SHA-256；`dissolveGroup` :419-434 删 upload 记录不删存储对象 → 孤儿累积。修复：存储 I/O 移出锁临界区；解散时收集 objectKey deleteQuietly。否。

**REV-045（P3 改进建议｜im-web）** `auth.ts` :64：JWT 明文 localStorage，而聊天缓存用 safeStorage 加密且设置页向用户宣称加密 —— 保护级别不一致。修复：桌面端 token 经主进程 safeStorage 存取。否。

**REV-046（P3 改进建议｜im-web）** `Chat.vue`：消息列表无虚拟滚动，长历史会话 DOM 数千节点卡顿；`handleSendMessage` :2985-3002 附件队列任一失败即 return，文本消息被静默扣留无提示。修复：虚拟滚动/页数上限；文本独立发送或提示。否。

**REV-047（P3 改进建议｜Electron）** `index.html`+`main.ts`：桌面端无 CSP（nginx CSP 对 file:// 无效），一旦未来出现 XSS 无兜底。修复：meta CSP 或 onHeadersReceived 注入。否。

**REV-048（P3 改进建议｜Electron）** `main.ts`：未设 `setPermissionRequestHandler`，渲染进程权限请求（摄像头/麦克风）默认放行。修复：注册默认拒绝的白名单 handler。否。

**REV-049（P3 改进建议｜Electron）** `main.ts` :400-440：messages:* IPC 的 userId 完全由渲染进程传入，不校验调用者 → 同机多账号时 XSS 可越权读/清他人本地缓存。修复：登录时主进程登记当前 userId，强制使用。否。

**REV-050（P3 改进建议｜im-admin）** `api/index.ts` :51-56：并发 401 无去抖，多条相同 toast；`baseURL` :10 硬编码 localhost:8080 不支持环境变量；`auth.ts` :15 JSON.parse 无 try/catch，损坏即白屏死循环。修复：跳转单例标志、VITE_API_BASE、parse 兜底。否。

**REV-051（P3 改进建议｜im-admin）** `ClientReleaseManage.vue` :175-178：forceUpdate 发布无二次确认（与 UI 自述流程不符）；多个 async 方法未 catch 形成 unhandled rejection。修复：强制更新追加确认；补 catch。否。

**REV-052（P3 改进建议｜契约）** `ConversationVO.java` :12-53 无 createTime/updateTime 字段，前端 createdAt 恒 ''（当前靠 lastMessageTime 兜底无可见 bug）。修复：VO 补字段。否。

**REV-053（P3 改进建议｜契约）** 分页接口 Result.success(PageResult) 双重包装靠前端 `records ?? data` 容错，无类型/测试保护；未来某接口直接返回 PageResult 时分页静默退化为首页。修复：统一约定+契约测试。否。

**REV-054（P3 已确认｜契约）** `Chat.vue` :3374 处理 `MESSAGE_SEND_REPLY` 指令，后端从未发送 → 死代码。修复：删除分支或后端补发。否。

**REV-055（P3 改进建议｜Docker）** `Dockerfile.backend` 基础镜像未 pin digest（Dockerfile.minio 已做双 SHA256 pin，标准不一）；`app/im-backend/` 缺 .dockerignore。否。

**REV-056（P3 改进建议｜配置）** multipart 128MB 与 FILE_CHUNK_SIZE/FILE_SMALL_FILE_MAX_SIZE 无启动一致性校验，运维调大即被 413 不友好拒绝。修复：启动 fail-fast 校验。否。

---

## 4. 高风险疑点清单

**REV-101（P2 疑点｜im-web/Electron）** `main.ts` :456-465 / `updater.ts` :482-488 / `runtime.ts` :39：渲染进程允许桌面端配任意 `http://` 服务器并正常聊天，但主进程文件下载与自动更新强制 HTTPS → http 部署时下载/更新静默失败且报错不指向真实原因。缺证据：现网是否纯 HTTPS 部署（官方 compose 仅 443，按文档部署不触发）。验证：配 http://IP:8080 测文件下载。

**REV-102（P2 疑点｜Electron 更新）** `updater.ts` :265-273,431-444 + `package.json` :39-67：更新信任链 = 用户手填 serverOrigin 的 TLS + latest.yml sha512（与安装包同源）；无代码签名配置（win 段无 certificateFile/publisherName）、无证书固定；forceUpdate 自动下载 + autoInstallOnAppQuit → 更新服务器被攻陷可向约 200 台终端零交互投递任意代码。已确认 TLS 校验未关闭（非已确认漏洞），属设计纵深缺口。验证：伪造更新源测试拒绝行为。

**REV-103（P2 疑点｜文档）** 文档 4.6/7.2/10.2 节描述登录日志、操作日志、消息审计、角色权限管理（sys_login_log/sys_role 等 5 表），代码中完全不存在且未标注"规划未实现" → 审计能力被误认为已有，企业合规盲区。缺证据：项目方是否内部明知为规划项。

**REV-104（P3 疑点｜后端-文件）** 历史迁移 20260525 曾把所有 expires_at IS NULL 文件（含老头像）置 7 天过期，20260604 只豁免会话文件 → 若生产执行过该迁移，老用户头像已 410。缺证据：生产库 `im_file` 中 status='EXPIRED' 且 conversation_id IS NULL 的记录数。

**REV-105（P3 疑点｜后端-消息）** 已读双轨制秒级缝隙：lastReadTime=now() + create_time 秒级精度，同秒到达的消息角标为 0 但 delivery.read_status=0 → "有未读消息但角标 0"。缺证据：客户端是否同时展示两种未读态。

**REV-106（P3 疑点｜后端-WS）** online:/presence: Redis 键无 TTL，断连清理与重连写入无顺序保护，进程崩溃留僵尸键（当前读路径以内存为准暂无实际误用）。

**REV-107（P3 疑点｜im-web）** `fileDownload.ts` :87-96：blob 回退分支 click 后同步 revokeObjectURL，部分浏览器可能下载失败/空文件。缺证据：目标浏览器实测。

**REV-108（P3 疑点｜CI）** desktop-release.yml 签名安装包上传 GitHub artifact（90 天），无法确认仓库 private/public；public 则"仅内网分发"的安装包对公网公开。

**REV-109（P3 疑点｜部署）** git 历史曾存在 MinIO 默认密码 `impassword`、JWT 默认值 `im-secret-key-needs-changing`（当前代码已清除且 CI 有拦截）。缺证据：现网 `.env` 是否已轮换。

**REV-110（P3 疑点｜im-admin）** localStorage 存 token+role，路由守卫角色判断可被本地篡改看到管理 UI（但后端 `/api/admin/**` 强制 ROLE_ADMIN，接口全 403，无实际越权）；当前无现存 XSS 汇。

---

## 5. 文档与代码不一致清单

| 编号 | 文档描述 | 实际代码 | 级别/状态 |
|---|---|---|---|
| REV-103 | 登录/操作日志、消息审计、角色权限 5 表（4.6/7.2/10.2 节） | 完全不存在 | P2 疑点 |
| REV-111 | "Java 21"（文档 L209、L782） | pom.xml:26 Java 17 | P2 已确认 |
| REV-112 | WS 协议：`/ws/im?token=`、扁平 MESSAGE_SEND、含 MESSAGE_RECALL 指令（6.5/8.2 节） | 实为一次性 ticket 握手、`{cmd,seq,data}` 嵌套、无 RECALL 命令（撤回走 HTTP）、有未记录的 MESSAGE_UPDATED/MESSAGE_READ | P2 已确认 |
| REV-113 | 会话类型含"3=系统通知"；消息类型含 AUDIO/VIDEO/SYSTEM/RECALL/REPLY/CARD；im_message 含 reply_msg_id/is_recalled 列；登录响应含 permissions；`GET /api/messages/history`、`GET /api/admin/users`；5.3 节 13 个后端模块；im-web 用 Element Plus/Naive UI；管理端用 ECharts | 会话仅 1/2；消息仅 TEXT/IMAGE/FILE/STICKER；无该两列；LoginResponse 无 permissions；实际为 `/api/messages/{id}`、`/api/admin/users/page`；实际 2 模块；im-web 无 UI 库；im-admin 无 echarts | P3 已确认 |

---

## 6. 缺失测试清单

后端已有 controller/security/service/websocket 测试目录，前端有 chat/attachmentQueue 等单测，但以下关键路径无覆盖：

1. CI 冒烟：空库 docker compose 起全栈跑通 Flyway（可直接防住 REV-001）。
2. 并发单聊创建唯一性（REV-003）、单聊退出后再发起（REV-004）。
3. 消息分页同秒/乱序 id 的分页稳定性（REV-005）。
4. 多端同账号消息同步（REV-006）。
5. WebSocketSessionManager remove/add 并发竞态（REV-007）。
6. 过期上传任务清理 vs completeTask 并发（REV-008）。
7. 部门删除/成环校验（REV-009/010，修复后需补）。
8. 登出后 chatStore 清空的 Pinia 单测（REV-002）。
9. Admin 前端字段与后端 VO 的契约测试（可防 REV-011/012 类回归）。
10. CI 检查项：workflow 输入注入静态检查、action pin 检查。

---

## 7. P0/P1 必须立即处理项

- REV-001（P1）：Flyway 迁移与 schema.sql 冲突 → 全新部署必 crash-loop。修复 schema.sql 基线 + 迁移幂等守卫 + CI 冒烟。
- REV-002（P1）：登出不清理 chatStore → 跨账号消息可见。chatStore 加 reset 并在登出调用。

无已确认 P0。

## 8. P2 建议近期处理项

- 后端业务：REV-003（单聊并发）、REV-004（单聊退出语义）、REV-005（分页游标）、REV-006（多端同步）、REV-007（SessionManager 竞态）、REV-008（上传清理竞态）。
- 部门/Admin：REV-009、REV-010、REV-011、REV-012、REV-013。
- 安全/部署：REV-014（登录防爆破）、REV-015（CORS）、REV-016（MinIO console）、REV-017（容器 root）、REV-018（CI 注入）。
- 性能：REV-019（本地消息缓存无界）。
- 文档：REV-111、REV-112（技术栈与 WS 协议文档）。
- 疑点待确认：REV-101、REV-102、REV-103。

## 9. P3 后续优化项

REV-020~REV-056（已确认/建议，见第 3 节简列）+ REV-104~REV-110（疑点）。优先挑：REV-024（冲突一律 500，影响管理员体验）、REV-023（长消息 500）、REV-021（已读口径）、REV-040/041（CI 加固，改动极小）。

## 10. 分阶段整改计划

- 第 1 阶段（本周，阻断性）：REV-001、REV-002。改 schema.sql + 迁移守卫 + CI 空库冒烟；chatStore reset。合计约 1 人日，回归：全新部署 + 换账号登录。
- 第 2 阶段（两周内，消息与会话正确性）：REV-003~REV-008 + REV-021。涉及单聊唯一键迁移、分页复合游标、多端推送、SessionManager 原子化、清理任务加锁。配 REV-020/022/025 一并修（同一代码区域）。补第 6 节测试 2-6。
- 第 3 阶段（两周内，Admin 与部门）：REV-009~REV-013、REV-024、REV-050/051。后端补校验与 VO，前端修字段绑定与 401 逻辑。
- 第 4 阶段（一个月内，安全与部署加固）：REV-014~REV-018、REV-101/102（确认后实施）、REV-016/017/039/040/041/055。轮换核查 REV-109。
- 第 5 阶段（持续，性能与文档）：REV-019（缓存分桶）、REV-043/044/046（性能）、REV-103/111/112/113（文档刷新，标注已实现/规划状态）、其余 P3 随迭代消化。

---

附：本报告所有"已确认"项均有代码行级证据且防护链已排查；"高风险疑点"均注明了缺失的确认证据。审查过程未修改任何文件。
